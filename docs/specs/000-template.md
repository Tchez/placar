# SPEC NNN — <title>

- **Status:** draft | ready | in progress | done
- **Created:** YYYY-MM-DD
- **Depends on:** SPEC NNN (or none)

## Goal

One paragraph: what changes for the user when this ships. Written in terms of the family
using the app, not in terms of code.

## Context

What an implementer needs to know that is not already in `README.md`. Inline the relevant
rules and decisions — do not point at the owner's vault, since the team cannot read it.

## Scope

**In**
- ...

**Out**
- ... (explicitly, so it does not get built by accident)

## Behaviour

The observable behaviour, screen by screen or flow by flow. Cover the empty state, the error
state, and what happens when the user corrects a mistake.

## Rules

Any counting, validation or edge-case rule this SPEC depends on, stated exactly. Ties,
negatives, zero, overflow past the target — say what happens.

## Acceptance criteria

Checkable statements. Each one either passes or fails; no "should feel fast".

- [ ] ...
- [ ] ...

## Open questions

Anything unresolved. **A SPEC with open questions is not `ready`** — resolve them or move them
to Out of scope before implementation starts.
